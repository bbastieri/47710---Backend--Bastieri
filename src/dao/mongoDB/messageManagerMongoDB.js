import { MessageModel } from "./models/messagesModel";

export default class MessageManagerMongoDB {

    async addMessage () {
        try {

        } catch (error){
            console.log(error)
        }
    }

    
}